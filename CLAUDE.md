# CLAUDE.md — Claude Code carrier for GUIDE SDD

Claude Code always loads this file. It **routes to the canonical always-loaded core** in `AGENTS.md`
(the agent-neutral carrier), so there is a single stub to maintain. Native `@`-import gives Claude
physical injection (Tier A). Mechanism map + capability tiers: `host-adapter.md`. Template — in a target
repo this sits at the repo root beside `AGENTS.md`; the spine lives at `sdd/`.

@AGENTS.md
